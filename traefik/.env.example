# Traefik environment variables
# Root CA bundle LEGO uses to trust the ACME server (step-ca here).
# Path is inside the container, from the ./traefik/certs mount.
LEGO_CA_CERTIFICATES=/etc/step/step-root-ca.crt
