TZ=America/Chicago
PAPERLESS_URL=http://paperless:8000
# Paperless API token: Profile > API Tokens > Generate
PAPERLESS_TOKEN=change-me
# Where export_csv writes files. Mounted from ./paperless/export in compose.
EXPORT_DIR=/export
# Caps on tool output, to keep responses inside the model's context window.
# Sized for a fan-out, not a single call: the model issues parallel
# get_document_content calls (observed 9 in one turn) despite the tool
# description telling it to read one at a time. Budget against llama-cpp's
# --ctx-size (65536 here) rather than a per-slot share: --kv-unified lets one
# request use the whole context. The conversation, tool schemas and the export
# payloads the model writes back all compete for the same budget - a job
# covering ten documents already exhausted 32768 tokens.
MAX_CONTENT_CHARS=6000
MAX_SEARCH_RESULTS=50
